# ============================================================================
# Infrastructure Environment Variables
# ============================================================================
# Copy this file to .env and fill in the required values for production.
# For development, defaults are used if .env is not present.
#
# Note: .env files should NOT be committed to version control.
# For production deployments, these values should be populated by Ansible
# or another configuration management tool using secrets from a vault.

# --- Quarkus Database (PostgreSQL) ---
QUARKUS_DB_USER=quarkus
QUARKUS_DB_PASSWORD=replace-me-quarkus-db-password
QUARKUS_DB_NAME=quarkusdb

# --- Keycloak Database (PostgreSQL) ---
KEYCLOAK_DB_USER=keycloak
KEYCLOAK_DB_PASSWORD=replace-me-keycloak-db-password
KEYCLOAK_DB_NAME=keycloakdb

# --- Keycloak Admin Credentials ---
KC_ADMIN_USER=admin
KC_ADMIN_PASSWORD=replace-me-keycloak-admin-password

# --- MinIO Admin Credentials ---
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=replace-me-minio-root-password
